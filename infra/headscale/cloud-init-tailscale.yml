#cloud-config
package_update: true
packages:
  - docker.io
  - docker-compose-v2
  - git

runcmd:
  - systemctl enable docker
  - systemctl start docker
  - git clone https://github.com/AJahueyM/Porebazu.git /opt/porebazu
  - docker compose -f /opt/porebazu/vpn/docker-compose-tailscale.yml up -d

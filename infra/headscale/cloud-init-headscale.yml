#cloud-config
package_update: true
packages:
  - docker.io
  - docker-compose
  - git

runcmd:
  - systemctl enable docker
  - systemctl start docker
  - git clone https://github.com/AJahueyM/Porebazu.git /opt/porebazu
  - cd /opt/porebazu/vpn && docker compose up -d
